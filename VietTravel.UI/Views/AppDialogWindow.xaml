<Window x:Class="VietTravel.UI.Views.AppDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Thông báo"
        Width="520"
        MinWidth="420"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Opacity="0"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Storyboard x:Key="DialogOpenStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0"
                             To="1"
                             Duration="0:0:0.18"/>
            <DoubleAnimation Storyboard.TargetName="DialogCardScale"
                             Storyboard.TargetProperty="ScaleX"
                             From="0.96"
                             To="1"
                             Duration="0:0:0.22">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="DialogCardScale"
                             Storyboard.TargetProperty="ScaleY"
                             From="0.96"
                             To="1"
                             Duration="0:0:0.22">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="DialogCardTranslate"
                             Storyboard.TargetProperty="Y"
                             From="8"
                             To="0"
                             Duration="0:0:0.22">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
            <BeginStoryboard Storyboard="{StaticResource DialogOpenStoryboard}"/>
        </EventTrigger>
    </Window.Triggers>

    <Grid Background="Transparent" Margin="20">
        <Border Background="{StaticResource CardBrush}" CornerRadius="18" Padding="24" BorderBrush="#11000000" BorderThickness="1"
                RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="DialogCardScale" ScaleX="0.96" ScaleY="0.96"/>
                    <TranslateTransform x:Name="DialogCardTranslate" Y="8"/>
                </TransformGroup>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect BlurRadius="28" ShadowDepth="8" Opacity="0.2" Color="Black" Direction="270"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Width="42" Height="42" CornerRadius="21" Background="#0F007AFF" Margin="0,0,12,0">
                        <materialDesign:PackIcon x:Name="DialogIcon"
                                                 Kind="InformationOutline"
                                                 Width="22"
                                                 Height="22"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"
                                                 Foreground="{StaticResource PrimaryBrush}"/>
                    </Border>

                    <TextBlock x:Name="DialogTitleText"
                               Grid.Column="1"
                               Text="Thông báo"
                               FontFamily="{StaticResource AppFont}"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"/>
                </Grid>

                <TextBlock x:Name="DialogMessageText"
                           Grid.Row="1"
                           Margin="0,14,0,0"
                           FontFamily="{StaticResource AppFont}"
                           FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           TextWrapping="Wrap"
                           LineHeight="22"/>

                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,20,0,0">
                    <Button x:Name="TertiaryButton"
                            Style="{StaticResource AppleGhostButton}"
                            Margin="0,0,10,0"
                            Visibility="Collapsed"/>
                    <Button x:Name="SecondaryButton"
                            Style="{StaticResource AppleGhostButton}"
                            Margin="0,0,10,0"
                            Visibility="Collapsed"/>
                    <Button x:Name="PrimaryButton"
                            Style="{StaticResource ApplePrimaryButton}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
