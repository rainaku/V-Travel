<Window x:Class="VietTravel.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Viet Travel" Height="920" Width="1360"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MinHeight="860" MinWidth="1200"
        ResizeMode="CanResizeWithGrip"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        PreviewKeyDown="Window_OnPreviewKeyDown">
    
    <Grid Margin="10">
        <!-- Shadow layer kept separate to avoid blurring all window content -->
        <Border Background="White" CornerRadius="16" IsHitTestVisible="False">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="2" Opacity="0.12" Direction="270"/>
            </Border.Effect>
        </Border>

        <Border Background="{StaticResource BackgroundBrush}" CornerRadius="16" SnapsToDevicePixels="True">
            <Grid x:Name="MainContentClipHost" SizeChanged="MainContentClipHost_SizeChanged" SnapsToDevicePixels="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="44"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Title Bar -->
                <Border Grid.Row="0" Background="Transparent" CornerRadius="16,16,0,0"
                        MouseLeftButtonDown="Header_MouseLeftButtonDown">
                    <Grid>
                        <!-- Traffic Light Buttons (macOS style) -->
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                            <Button Click="Close_Click" Width="14" Height="14" Margin="0,0,8,0" Cursor="Hand"
                                    ToolTip="Đóng">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Ellipse x:Name="dot" Fill="#FF5F57" Width="14" Height="14" Stroke="#E04840" StrokeThickness="0.5"/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="dot" Property="Fill" Value="#FF3B30"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button Click="Minimize_Click" Width="14" Height="14" Margin="0,0,8,0" Cursor="Hand"
                                    ToolTip="Thu nhỏ">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Ellipse x:Name="dot" Fill="#FEBC2E" Width="14" Height="14" Stroke="#DEA123" StrokeThickness="0.5"/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="dot" Property="Fill" Value="#FF9500"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button Click="Maximize_Click" Width="14" Height="14" Cursor="Hand"
                                    ToolTip="Phóng to">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Ellipse x:Name="dot" Fill="#28C840" Width="14" Height="14" Stroke="#1DAD2B" StrokeThickness="0.5"/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="dot" Property="Fill" Value="#34C759"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                        
                        <!-- App Title -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Source="/UI/Assets/logo.png" Width="18" Height="18" Margin="0,0,6,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Text="Viet Travel" VerticalAlignment="Center"
                                       FontFamily="{StaticResource AppFont}" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Content Area -->
                <ContentControl Grid.Row="1" x:Name="MainContent" Content="{Binding CurrentViewModel}" SnapsToDevicePixels="True"/>

                <materialDesign:Snackbar Grid.RowSpan="2"
                                         MessageQueue="{Binding SnackbarMessageQueue}"
                                         HorizontalAlignment="Right"
                                         VerticalAlignment="Bottom"
                                         Margin="0,0,24,24"/>
            </Grid>
        </Border>
    </Grid>
</Window>

